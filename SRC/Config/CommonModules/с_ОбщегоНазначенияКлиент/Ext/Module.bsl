 // ++ Ситис: p.virich   2018-07-08
Процедура УстановитьЗаголовокИнформационнойБазы() Экспорт
	Если с_ОбщегоНазначенияВызовСервера.ЭтоРабочаяБаза() Тогда
		Возврат;
	КонецЕсли;
	ИмяИнформационнойБазы = с_ОбщегоНазначенияВызовСервера.ПолучитьИмяИнформационнойБазы();
	УстановитьКраткийЗаголовокПриложения("(! "+ИмяИнформационнойБазы+" !)");
КонецПроцедуры  // -- Ситис: p.virich   2018-07-08 